using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Inicializar : MonoBehaviour
{
 private DeckController deckController;
    private GameObject drawnCard;
  
   
     void Start()
    {
        //deckController = GameObject.FindObjectOfType<DeckController>();

    //GameObject selectedCardPrefab = deckController.SelectRandomObject(); 
        //GameObject selectedCard = Instantiate(selectedCardPrefab, transform.position, Quaternion.identity);
        //selectedCard.transform.SetParent(transform);


    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
